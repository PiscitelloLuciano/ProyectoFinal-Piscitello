import { FontSizeTitleDirective } from './font-size-title.directive';

describe('FontSizeTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new FontSizeTitleDirective();
    expect(directive).toBeTruthy();
  });
});
